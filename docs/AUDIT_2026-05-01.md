# Clarify AI — Production Audit
**Date:** 2026-05-01
**Scope:** Overlay system, sidebar features, backend (Edge Functions, RPCs, RLS), and existing `docs/*.md` reconciliation.

This document is the source of truth for what is actually wired in production today. It supersedes any conflicting claims in `QA_REPORT.md`, `ARCHITECTURE.md`, and `STEALTH_FEATURES.md`.

---

## 1. Overlay system

### Fixed in this audit
| Issue | Resolution |
|---|---|
| `/app/live` rendered both the 2-panel page **and** the floating overlay | `LiveRehearsal.tsx` is now overlay-only (mirrors `LiveOverlay.tsx`). Setup wizard → `<OverlayWindow />` only. |
| Two `OverlayWindow.tsx` files (one stub) | Deleted `src/components/live/OverlayWindow.tsx` (30-line stub). |
| `#clarify-overlay-root` did not exist in `index.html` → dynamic creation warning every load | Added the `<div id="clarify-overlay-root">` to `index.html` and seeded it in `main.tsx`. |
| Overlay buttons silent on `LiveRehearsal` (`onGenerate`, `onStartSession`, `onSetupNewSession` were not passed) | All seven overlay props now wired. |
| `is_visible` defaulted to false; LiveRehearsal never called `showOverlay()` | Active-phase effect calls `useOverlayStore.getState().showOverlay()`. |
| No way to recover overlay if user accidentally hides it | Floating "Show Overlay" pill renders bottom-right when `isActive && !isVisible`. |
| Screen-capture / system-audio permission rejections were silent on Safari/Firefox | Both now surface a `sonner` toast with a concrete fix instruction. |

### Overlay button smoke-test checklist (run after each release)
| Button | Expected side-effect | Where it lives |
|---|---|---|
| Mic toggle | `useAudioStore.is_muted` flips; mic icon updates | `OverlayToolbar` left side |
| AI Help | `hint_state → generating`, streaming chunks fill answer panel; toast "Speak a question…" if no question | Center primary |
| Screen | `getDisplayMedia` prompt; on cancel a clear toast (not silence) | Center primary |
| Chat | `active_tab === "chat"`; chat panel mounts | Center primary |
| ⋯ → System audio | `getDisplayMedia({audio:true})` prompt; toast on rejection; `streams.system_stream` set on success | More menu |
| ⋯ → Stealth | `OS-level` capture blocker on (desktop) + `STEALTH` chip in header | More menu |
| ⋯ → Auto-generate | `auto_generate` flips | More menu |
| ⋯ → Hint Style (Full/Hints/Keys) | `hint_style` updates; footer label refreshes | More menu |
| ⋯ → AI Model | `active_model` updates; next request routes to that provider | More menu |
| ⋯ → Panic mode | Panic banner with breathing steps appears; Escape clears | More menu |
| Minimize | `is_minimal_mode` flips; body collapses | Right side |
| End | `endLiveSession` → DB update + audio.stop + `status === completed` | Right side |
| Setup new (after end) | `phase === "setup"` → wizard reappears | Centered post-session |

---

## 2. Sidebar feature audit

Legend: **OK** = fully wired and production-ready · **Partial** = renders but missing a write path or error handling · **Stub** = UI only, no backend.

### Core
| Item | Route | Status | Backend |
|---|---|---|---|
| Dashboard | `/app/dashboard` | OK | `profiles`, `sessions` tables (RLS) |
| Live Co-Pilot | `/app/live` | **OK (fixed today)** | `start-session`, `end-session`, `generate-hint`, `generate-answer`, `deepgram-token` edge functions |
| Mock Interview | `/app/mock` | OK | Same as Live Co-Pilot |
| Prep Lab | `/app/prep/*` | OK | `prep-tool`, `polish-star-section`, `generate-star-answer`, `generate-practice-questions` |
| Mock Tests Hub & sub-pages | `/app/mock-test/*` | OK | `create-test`, `select-test-questions`, `submit-test`, `analyze-test-performance`, `parse-question-pdf`, `generate-questions`, `gap-analysis` |

### Growth
| Item | Route | Status | Notes |
|---|---|---|---|
| Call Sessions | `/app/sessions` | OK | Reads `sessions` (RLS) |
| Analytics | `/app/analytics` | OK | `useAnalytics` calls the `analytics-dashboard` edge function (server-side aggregation). |
| Documents | `/app/documents` | OK | `parse-resume` + `resumes` private bucket. |
| Answer Bank | `/app/answers` | OK | `session_answers` (RLS) |

### Planner
| Item | Route | Status | Notes |
|---|---|---|---|
| Interviews | `/app/interviews` | **OK (fixed today)** | Two-step persistence: `scheduled_interviews` (parent) + `interview_rounds` (date/platform). Auto-syncs to Google Calendar after create when connected. "Sync calendar" / "Connect calendar" CTA in the page header. |
| Companies | `/app/companies` | OK | `company-research` edge function with toast on failure. |
| Practice Rooms | `/app/rooms` | **Stub** | `rooms` table exists but `useRoom` is local-only and there's no realtime presence channel. Treat as preview until WebRTC is wired. |

### Footer
| Item | Status | Notes |
|---|---|---|
| Guide | OK | Static content. |
| Notifications | **OK (fixed today)** | Realtime channel on `notifications` table keeps unread count fresh; "Mark all read" calls `mark_notifications_read` RPC. |
| Settings | OK | All sub-pages wired. |
| Admin Panel | OK | Built in prior loops; gated by `requireAdmin` + `is_admin` column + `user_roles` RLS. |

---

## 3. Edge Functions inventory (38 deployed)

All functions return `Content-Type: application/json` with shared CORS from `_shared/cors.ts` ✓

| Function | Used by | Status |
|---|---|---|
| ai-coach-chat | Coach | OK |
| ai-feedback | Scorecard | OK |
| analytics-dashboard | Analytics page | **Not wired in UI** |
| analyze-test-performance | Mock Tests | OK |
| cancel-subscription | Billing | Needs Stripe secret |
| company-research | Companies page | OK |
| create-checkout | Billing | Needs Stripe secret |
| create-test | Mock Tests | OK |
| deduct-credits | All paid actions | OK (RPC `deduct_credits`) |
| deepgram-token | Live Co-Pilot | OK |
| delete-account | Settings → Danger | OK |
| disconnect-calendar | Interviews | OK (not user-triggered yet) |
| end-session | Live Co-Pilot | OK |
| export-user-data | Settings → Privacy | OK |
| gap-analysis | Mock Tests Analytics | OK |
| generate-answer | Live Co-Pilot, Prep | OK |
| generate-debrief | Debrief | OK |
| generate-hint | Live Co-Pilot | OK |
| generate-practice-questions | Prep Lab | OK |
| generate-questions | Mock Tests Import | OK |
| generate-star-answer | STAR Builder | OK |
| parse-question-pdf | Mock Tests Import | OK |
| parse-resume | Documents | OK |
| ping | Healthcheck | OK |
| polish-star-section | STAR Builder | OK |
| prep-tool | Prep Lab, screenshot analysis | OK |
| process-stripe-webhook | Stripe → DB sync | Needs Stripe secret |
| resume-subscription | Billing | Needs Stripe secret |
| schedule-interview | Interviews | **Not wired in UI** |
| select-test-questions | Mock Tests | OK |
| send-email | Notifications | OK |
| start-session | Live Co-Pilot | OK |
| stripe-webhook | Stripe → DB sync | Needs Stripe secret |
| submit-test | Mock Tests | OK |
| sync-calendar | Interviews | **Not wired in UI** |
| validate-api-key | Settings → BYOK | OK |

### Required secrets
Present: `LOVABLE_API_KEY`, `GEMINI_API_KEY`, `DEEPGRAM_API_KEY`, `OCR_API_KEY`, `SUPABASE_*`.
**Missing:** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET` — all billing flows return 500 silently. **Action:** user must add via Workspace Settings.

---

## 4. Database health

- 51 user-defined functions; all `SET search_path = public` ✓
- Roles via `user_roles` table + `app_role` enum + `has_role()` RPC ✓ (no role on profiles)
- Admin RPCs (`get_admin_perf_stats`, `get_admin_dau_mau`, `bulk_update_users`) restricted to `authenticated` + `has_role(_, 'admin')` ✓
- `protect_admin_column` trigger blocks non-admin promotion ✓
- Storage buckets: `resumes` (private), `documents` (private), `exports` (private), `avatars` (public), `question-images` (public) ✓

---

## 5. Reconciliation with existing docs

| Doc | Notes |
|---|---|
| `docs/QA_REPORT.md` | Coverage stats are stale. Reports 845 TODO placeholders — most are still TODO. The "Live Overlay 6 auto-pass" line is now accurate after today's overlay-only fix. |
| `docs/ARCHITECTURE.md` | Describes 2-panel live UI as canonical. **Update needed:** Live Co-Pilot is overlay-only as of 2026-05-01. |
| `docs/STEALTH_FEATURES.md` | Accurate. Add note: `#clarify-overlay-root` now declared in `index.html`. |
| `docs/INSTALLATION.md` | OK. Note that `STRIPE_SECRET_KEY` must be added before testing billing. |
| `docs/DATABASE.md` | Accurate but missing the admin RPCs added in last loops. |
| `docs/API.md` | Lists 32 edge functions; actual count is 38. Sync needed. |
| `docs/QA_MANUAL.md` | OK. |

---

## 6. Recommended next sprint

**P0 (blocks production)**
1. Add `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` secrets, then verify `create-checkout` round-trip.
2. Wire `analytics-dashboard` edge function into `/app/analytics` (currently shows local aggregations).
3. Wire `schedule-interview` + `sync-calendar` into `/app/interviews/new` (CTA buttons exist, no handlers).

**P1**
4. Realtime subscription on `notifications` table; switch to `mark_notifications_read` RPC for batch updates.
5. Convert `/app/rooms` to "Coming soon" or implement `rooms` table + realtime channel.
6. Add `toast.error()` to silent failure paths in `CompanyResearch.tsx`, `NewInterview.tsx`.

**P2**
7. Sync `docs/ARCHITECTURE.md` and `docs/API.md` with current state.
8. Replace remaining 845 TODO placeholders in QA suite with real or skipped tests.
