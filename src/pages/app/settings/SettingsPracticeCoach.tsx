import { Navigate } from "react-router-dom";

/** Settings shortcut — full guide lives at /app/guide/practice-coach */
export default function SettingsPracticeCoach() {
  return <Navigate to="/app/guide/practice-coach" replace />;
}
